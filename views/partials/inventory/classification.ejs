
<% } else { %>
  <%- res.redirect('/') %>
<% } %>

<%# messages() %> <!-- Placeholder for future messages -->

<% if (inventory.length > 0) { %>
    <div class="inventory-grid">
        <% inventory.forEach(vehicle => { %>
            <div class="vehicle-item">
              
                <a href="/inv/detail/<%= vehicle.inventory_id %>" title="View <%= vehicle.inv_make %> <%= vehicle.inv_model %>">
                    <img src="<%= vehicle.inv_thumbnail %>" alt="<%= vehicle.inv_make %> <%= vehicle.inv_model %>">
                </a>
                <h3><%= vehicle.inv_make %> <%= vehicle.inv_model %></h3>
                <p class="price">Price: $<%= vehicle.inv_price.toLocaleString() %></p>
            </div>
        <% }) %>
    </div>
<% } else { %>
    <p class="notice">Sorry, no vehicles were found in this category.</p>
<% } %>
